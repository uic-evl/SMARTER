"use strict"

var App = App || {};

let NomogramKnnController = function() {

    function selectKnn(selected) {
        if (selected) { // check knn ckechbox to draw knn nomogram
            // update nomogram with the knn mode
            // App.views.nomogram.update();

        } else { // normal nomogram
            // update nomogram with the normal mode
            // App.views.nomogram.update();
        }
    }


    return {

    };
}
